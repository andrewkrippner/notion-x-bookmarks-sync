{
  "name": "notion-x-bookmarks-sync",
  "version": "0.1.0",
  "description": "Sync your X bookmarks into a Notion database, with author, type, and full post text on every page.",
  "type": "module",
  "packageManager": "pnpm@10.18.3",
  "main": "dist/index.js",
  "license": "MIT",
  "author": "Andrew Krippner",
  "homepage": "https://github.com/andrewkrippner/notion-x-bookmarks-sync#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/andrewkrippner/notion-x-bookmarks-sync.git"
  },
  "bugs": {
    "url": "https://github.com/andrewkrippner/notion-x-bookmarks-sync/issues"
  },
  "keywords": [
    "notion",
    "x",
    "bookmarks",
    "notion-worker",
    "notion-sync"
  ],
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit",
    "oauth": "node scripts/x-oauth.mjs",
    "worker:create": "tsc && ntn workers deploy --name notion-x-bookmarks-sync",
    "worker:deploy": "tsc && ntn workers deploy",
    "worker:sync": "ntn workers sync trigger xBookmarksSync",
    "worker:sync:preview": "ntn workers sync trigger xBookmarksSync --preview",
    "worker:status": "ntn workers sync status",
    "worker:reset": "ntn workers sync state reset xBookmarksSync",
    "worktree:setup": "bash worktree-setup.sh",
    "worktree:archive": "bash worktree-archive.sh"
  },
  "dependencies": {
    "@notionhq/workers": "^0.4.0"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3"
  },
  "engines": {
    "node": ">=22"
  }
}
